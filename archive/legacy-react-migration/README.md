# Legacy Vanilla JS Application (pre-React migration)

Archived during the CareMarshall React rewrite (v0.8).

## Contents

- `caremarshall-full.html` — Original 5,500-line SPA shell
- `reports.html` — Standalone reports dashboard (beekeeping-era charts)
- `app-homecare-firebase.js` — Alternate Firebase demo
- `docs-js/js/` — All vanilla JavaScript modules (core.js, sites.js, etc.)
- `docs-css/css/` — Original brand.css
- `REPORTS_README.md` — Legacy reports documentation

## Replacement

The React app in `app/` replaces this code. Built output deploys to `docs/`.

Offline queue key `homeCarePendingChanges` is preserved for backward compatibility.
